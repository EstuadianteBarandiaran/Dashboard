<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Receta Médica</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 20px;
            position: relative;
        }

        .logo-fondo {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0.2;
            z-index: -1;
            background-image: url('<%= logo %>');
            background-repeat: no-repeat;
            background-position: center;
            background-size: contain;
        }

        .logo-esquina {
            position: absolute;
            top: 20px;
            left: 20px;
            width: 100px;
            height: auto;
            z-index: 1;
        }

        .encabezado {
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            position: relative;
            margin-left: 120px; /* Espacio para el logo en esquina */
        }

        .info-doctor {
            flex-grow: 1;
            margin-left: 20px;
        }

        .decoracion {
            width: 100%;
            height: 5px;
            background-color: blue;
            margin: 10px 0;
        }

        .fecha {
            text-align: right;
            margin-bottom: 10px;
        }

        .contenido {
            padding: 20px;
            background-color: rgba(255, 255, 255, 0); /* Fondo completamente transparente */
        }

        .firma {
            text-align: right;
            margin-top: 20px;
            position: relative;
        }

        .linea-firma {
            width: 200px;
            height: 1px;
            background-color: #000;
            margin: 20px 0 10px auto;
        }
    </style>
</head>
<body>
    <div class="logo-fondo"></div>
    <img class="logo-esquina" src="<%= logo %>" alt="Logo">

    <div class="encabezado">
        <div class="info-doctor">
            <p>Dr. <%= doctor.nombre %></p>
            <p><%= clinica.direccion %></p>
            <p>Teléfono: <%= clinica.telefono %></p>
        </div>
    </div>

    <div class="decoracion"></div>

    <div class="fecha">
        <p><%= fecha %></p>
    </div>

    <div class="contenido">
        <h3>DATOS DEL PACIENTE</h3>
        <p><strong>Nombre:</strong> <%= paciente.nombre || 'No especificado' %></p>
        <p><strong>Motivo de consulta:</strong> <%= paciente.motivo || 'No especificado' %></p>
        <p><strong>Síntomas:</strong> <%= paciente.sintomas || 'No especificado' %></p>

    <div class="diagnostico-section">
        <h3 class="section-title">DIAGNÓSTICO</h3>
        <p><%= diagnostico %></p> <!-- Usa directamente 'diagnostico' -->
    </div>

    <div class="tratamiento-section">
        <h3 class="section-title">TRATAMIENTO</h3>
        <p><%= tratamiento %></p> <!-- Usa directamente 'tratamiento' -->
    </div>
</div>

    <div class="linea-firma"></div>
    <div class="firma">
        <p>Dr. <%= doctor.nombre %></p>
        <p>Lic. Méd. <%= doctor.licencia || '' %></p>
    </div>

    <!-- Modal para vista previa -->

    <script>

        // Event listeners para el modal
        document.addEventListener('DOMContentLoaded', () => {
            const modal = document.getElementById('pdf-preview-modal');
            const closeBtn = document.querySelector('.close-btn');
            
            closeBtn.addEventListener('click', () => {
                modal.style.display = 'none';
            });
            
            modal.addEventListener('click', (e) => {
                if (e.target === modal) {
                    modal.style.display = 'none';
                }
            });
        });
    </script>
</body>
</html>